# Content TODO

The following factual claims and placeholders need to be confirmed and updated before production launch:

1. **Retention Period**: How long are unverified names/phones stored? How long are incident reports retained? (Needs confirmation).
2. **SMS Provider Name**: The name of the specific SMS provider used in production (e.g., Twilio, SNS, custom local gateway). Currently using a simulated mock for the demo.
3. **AI Provider Name**: The specific AI model/provider used for triage (e.g., OpenAI, Anthropic) if required to be explicitly named for transparency.
4. **IP Addresses**: Clarify exactly whether and how long IP addresses are stored for abuse prevention. 
5. **Unbuilt Features Claims**: 
   - Android SOS App (automatic offline sending via background SMS).
   - Walkie-talkie voice channel integration.
   - (Ensure these are clearly labeled as "Planned" wherever mentioned in marketing copy, and update them when they actually ship).
